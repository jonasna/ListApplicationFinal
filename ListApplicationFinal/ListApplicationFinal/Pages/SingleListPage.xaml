<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:xForms="clr-namespace:Syncfusion.ListView.XForms;assembly=Syncfusion.SfListView.XForms"
             xmlns:buttons="clr-namespace:Syncfusion.XForms.Buttons;assembly=Syncfusion.Buttons.XForms"
             x:Class="ListApplicationFinal.Pages.SingleListPage"
             BackgroundColor="{StaticResource PrimaryBackgroundColor}">

    <xForms:SfListView x:Name="ToDoListView"
                       ItemsSource="{Binding ToDoCollection}"
                       TapCommand="{Binding ItemTappedCommand}"
                       >

        <xForms:SfListView.ItemTemplate>

            <DataTemplate>

                <Grid>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>                    

                    <Label Text="{Binding Name}" Grid.Column="0" HorizontalOptions="CenterAndExpand" VerticalOptions="Center"/>

                    <Label Text="{Binding Description}" Grid.Column="1" HorizontalOptions="CenterAndExpand" VerticalOptions="Center"/>

                    <buttons:SfCheckBox Grid.Column="2" IsChecked="{Binding Complete}" HorizontalOptions="CenterAndExpand" VerticalOptions="Center" InputTransparent="True"/>

                </Grid>

            </DataTemplate>

        </xForms:SfListView.ItemTemplate>

    </xForms:SfListView>

</ContentPage>